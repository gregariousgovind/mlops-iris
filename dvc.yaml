stages:
  prepare_data:
    desc: "Load Iris, write raw/processed CSV + schema + metadata"
    cmd: python src/data.py
    deps:
      - src/data.py
    outs:
      - data/raw/iris.csv
      - data/processed/iris.csv
      - data/schema.json
      - data/metadata.json
